# Leave this empty to use the same-origin Sites API (D1 + R2).
NEXT_PUBLIC_API_BASE_URL=

# Optional: set to "local" for the browser-only learning demo.
NEXT_PUBLIC_DEMO_MODE=

# Set to the trusted deployed origin when building outside Sites.
SITE_URL=http://localhost:3000
