{
  "name": "issueflow-learning-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "node scripts/check-curriculum.mjs && vitepress build docs && node scripts/check-doc-site-links.mjs",
    "docs:check-curriculum": "node scripts/check-curriculum.mjs",
    "docs:check-links": "node scripts/check-doc-site-links.mjs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  }
}
